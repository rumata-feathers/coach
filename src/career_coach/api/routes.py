"""Placeholder for conversation and admin routes.

Real endpoints (``POST /users``, ``POST /chat``, ``POST /admin/distill/{user_id}``)
are added in Task 9. Kept as an empty module so the package layout matches
``SPEC.md`` §9 from Task 1 onwards.
"""
